git init -> inicializa um repositorio local do git

git add <arquivo> -> adiciona na branch do repositório local -> torna o arquivo passivo de ser vigiado / commitado pelo git

git status -> passa a situação atual do arquivos e pastas do repositório local

git commit -> ele salva o que foi desenvolvido no momento, tornando passível de retornar aquele estado do código.